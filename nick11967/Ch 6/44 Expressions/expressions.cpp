// UVa ID: 10157

#include <bits/stdc++.h>

#define MAX_LEN 300
#define MAX_DEP 150

int exp[MAX_LEN + 1][MAX_DEP + 1];

using namespace std;

int main()
{
    int n, d;
    while (cin >> n >> d)
    {
        if (n % 2 == 1)
        {
            printf("0\n");
            break;
        }
    }
    return 0;
}